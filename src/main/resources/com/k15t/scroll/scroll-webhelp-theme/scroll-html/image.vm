#if($renderStart)
    <img ${idAttribute} ${classAttribute} src="$stringEscapeUtils.escapeHtml($imageFilename)" alt="$stringEscapeUtils.escapeHtml($imageFilename)"#if($element.width) width="$element.width"#end />
    #if($element.title)
        <br/><span class="caption">$stringEscapeUtils.escapeHtml($element.title)</span>
    #end
#end